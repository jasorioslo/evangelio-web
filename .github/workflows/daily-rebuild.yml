name: Daily rebuild

on:
  schedule:
    # 6:00 UTC = medianoche hora de Mexico (CST, UTC-6)
    - cron: '0 6 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  rebuild:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Fetch YouTube video IDs from channel RSS
        run: python3 scripts/fetch_youtube_ids.py
        continue-on-error: true

      - name: Commit (with or without YouTube updates)
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit --allow-empty -m "Daily rebuild $(date -u +%Y-%m-%d)"
          git push